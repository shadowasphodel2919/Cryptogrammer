import { Link } from "react-router-dom";

export const Algorithms = () => {
    return (<div>
        <section>
            <Link as={Link} to="caeser">Caeser Cipher</Link>
        </section>
    </div>);
}